# parkinson-tremor-detection
Parkinson’s tremor detection and human-activity recognition from wearable accelerometer data — preprocessing, sliding-window feature extraction, and classical ML classifiers (SVM, RandomForest).
